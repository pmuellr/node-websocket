node-websocket
================================================================================

simple websocket tester for node

Run as:

    node server.js

You can also push this project to a Cloud Foundry instance with:

    cf push
  
